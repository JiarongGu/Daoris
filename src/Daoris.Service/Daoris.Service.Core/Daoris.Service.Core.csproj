<Project Sdk="Microsoft.NET.Sdk">

  <!-- Ingest, classification and query. No package dependencies: the first slices need no
       embeddings, no vector store and no model, and adding them before a query needs them pays
       their cost for nothing. The cognition sibling arrives with semantic search (D22). -->
  <PropertyGroup>
    <RootNamespace>Daoris.Knowledge</RootNamespace>
  </PropertyGroup>

</Project>
