# Archive — superseded documents, kept for the reasoning

Nothing here describes the project as it is now. Each document was true when it was written and has
been overtaken; they are kept because the *reasoning* in them explains choices the current documents
only state.

| Document | What it was | What replaced it |
|---|---|---|
| `2026-08-01-framework-note.md` | The original sketch: fourteen .NET packages across five pillars | `docs/DECISIONS.md` D1 — six of those already shipped in a sibling, so building them again would have produced a second worse copy. Most of the rest were premature or belonged elsewhere; naming that early is what kept the first version small enough to finish |
| `2026-08-04-daoris-v0.1-plan.md` | The task-by-task execution plan for the CLI, written before any code | Fully executed. Outcomes are in `docs/task-archive.md`, and every decision it made is in `docs/DECISIONS.md` |

Read `docs/2026-08-04-daoris-design.md` for the contract, `docs/DECISIONS.md` for why, and
`ROADMAP.md` for what is next.
